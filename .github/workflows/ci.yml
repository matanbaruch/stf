name: CI

on:
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:

# Every job that needs more than this says so itself. The report job already
# scopes pull-requests: write to itself; widening it here would hand write
# access to the seven job types that only ever read, the Android legs included.
# It would not fix the 403 it looks like it fixes either: that one only happens
# on pull requests from forks, where GitHub hands out a read-only token whatever
# this block says.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  # Bump to invalidate the dependency cache without touching package.json.
  CACHE_EPOCH: v3

jobs:
  # Reads .github/android-matrix.json so the matrix and the report's coverage
  # check cannot drift apart.
  plan:
    name: Plan
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
    steps:
      - uses: actions/checkout@v7

      - name: Read the Android matrix
        id: plan
        run: node .github/scripts/plan-matrix.js

  build:
    name: Build
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    outputs:
      cache-key: ${{ steps.cachekey.outputs.key }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Configure a fast apt mirror
        uses: vegardit/fast-apt-mirror.sh@806c59276adfff2df420124ad5e4587f018c358a
        continue-on-error: true

      - name: Install system dependencies
        timeout-minutes: 12
        run: |
          bash .github/scripts/apt-install.sh \
            build-essential cmake ninja-build pkg-config python3 \
            libzmq3-dev libprotobuf-dev protobuf-compiler \
            graphicsmagick yasm nasm git curl zip unzip wget

      - name: Compute dependency cache key
        id: cachekey
        run: |
          echo "key=stf-deps-${CACHE_EPOCH}-${{ runner.os }}-${{ hashFiles('package.json', '.nvmrc') }}" >> "$GITHUB_OUTPUT"

      - name: Restore dependency cache
        id: deps
        uses: actions/cache@v6
        with:
          path: |
            node_modules
          key: ${{ steps.cachekey.outputs.key }}

      - name: Install npm dependencies
        if: steps.deps.outputs.cache-hit != 'true'
        run: npm install --no-audit --no-fund --loglevel=http

      # gulp's own `build` task races clean against webpack:build, so run the
      # two halves in order instead.
      - name: Build front-end bundle
        run: |
          npx gulp clean
          npx gulp webpack:build

      - name: Smoke check the CLI
        run: ./bin/stf -V

      - name: Verify the bundle exists
        run: |
          test -f res/build/entry/app.entry.js
          test -f res/build/entry/authmock.entry.js
          du -sh res/build

      - name: Upload front-end bundle
        uses: actions/upload-artifact@v7
        with:
          name: stf-bundle
          path: res/build
          retention-days: 3
          if-no-files-found: error

      - name: Record verdict
        if: always()
        run: |
          bash .github/scripts/verdict.sh build 'Build (npm + webpack)' \
            '${{ job.status }}' "deps cache ${{ steps.deps.outputs.cache-hit == 'true' && 'hit' || 'miss' }}"

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-build
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  lint:
    name: Lint
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Restore dependency cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
          key: ${{ needs.build.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: eslint + jsonlint
        id: lint
        run: npm run lint

      # A `node -e '...'` step is a single-quoted shell string, so an apostrophe
      # inside the program ends it and hands the rest to bash. The YAML stays
      # valid, so only running the step finds it. bash -n finds it here instead.
      - name: 'Shell-parse every run: block'
        id: runblocks
        if: always()
        run: node .github/scripts/check-run-blocks.js

      - name: Record verdict
        if: always()
        run: |
          bash .github/scripts/verdict.sh lint \
            'Lint (eslint + jsonlint + workflow shell)' \
            '${{ job.status }}' \
            'lib/, res/ and root JS + JSON, and every workflow run: block'

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-lint
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  unit:
    name: Unit tests
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Restore dependency cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
          key: ${{ needs.build.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: mocha unit specs
        id: unit
        run: npm run test:unit

      - name: Record verdict
        if: always()
        run: |
          bash .github/scripts/verdict.sh unit 'Unit tests (mocha)' \
            '${{ steps.unit.outcome || job.status }}' 'lib/util, lib/wire and lib/db specs'

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-unit
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  component:
    name: Component tests
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Restore dependency cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
          key: ${{ needs.build.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: vitest (res/app React specs)
        id: component
        run: npm run test:component -- --reporter=default --reporter=junit --outputFile.junit=../../test-results/vitest/junit.xml

      - name: Summarise vitest results
        if: always()
        id: vitest
        run: |
          if [ -f test-results/vitest/junit.xml ]; then
            node -e '
              var fs = require("fs")
              var xml = fs.readFileSync("test-results/vitest/junit.xml", "utf8")
              var pick = function(name) {
                var m = new RegExp(name + "=\"(\\d+)\"").exec(xml)
                return m ? Number(m[1]) : 0
              }
              var line = pick("tests") + " specs, " + pick("failures") +
                " failed, " + pick("errors") + " errored"
              fs.appendFileSync(process.env.GITHUB_OUTPUT, "summary=" + line + "\n")
            '
          else
            echo "summary=no junit output produced" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload vitest results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: vitest-results
          path: test-results/vitest
          retention-days: 3
          if-no-files-found: warn

      - name: Record verdict
        if: always()
        run: |
          bash .github/scripts/verdict.sh component 'Component tests (vitest + React)' \
            '${{ steps.component.outcome || job.status }}' '${{ steps.vitest.outputs.summary }}'

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-component
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  integration:
    name: Integration + device-less E2E
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    services:
      rethinkdb:
        image: rethinkdb:2.4.2
        ports:
          - 28015:28015
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Configure a fast apt mirror
        uses: vegardit/fast-apt-mirror.sh@806c59276adfff2df420124ad5e4587f018c358a
        continue-on-error: true

      - name: Install runtime dependencies
        timeout-minutes: 12
        run: bash .github/scripts/apt-install.sh graphicsmagick libzmq5

      - name: Restore dependency cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
          key: ${{ needs.build.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: Download front-end bundle
        uses: actions/download-artifact@v8
        with:
          name: stf-bundle
          path: res/build

      - name: Put adb on PATH
        run: |
          if [ -d "${ANDROID_HOME:-/usr/local/lib/android/sdk}/platform-tools" ]; then
            echo "${ANDROID_HOME:-/usr/local/lib/android/sdk}/platform-tools" >> "$GITHUB_PATH"
          else
            bash .github/scripts/apt-install.sh adb
          fi

      - name: Wait for RethinkDB
        run: bash .github/scripts/wait-for-port.sh 28015 60 rethinkdb

      - name: stf doctor
        run: node lib/cli/index.js doctor || true

      - name: Start stf local
        id: start
        env:
          STF_LOG_DIR: stf-logs
        run: bash .github/scripts/start-stf.sh

      - name: Create fake devices and read them back through STF's db layer
        id: fakedevice
        run: |
          node lib/cli/index.js generate-fake-device 'Nexus 5' -n 2
          node .github/scripts/stf-devices.js wait-count 2 120
          node .github/scripts/stf-devices.js list

      # `playwright install --with-deps` shells out to apt for the browser
      # libraries, so it inherits every apt flake the runtime steps have and
      # reports it as a bare "Installation process exited with code: 100".
      # Both halves are pure network, so retry rather than red the run.
      - name: Install Playwright
        timeout-minutes: 15
        run: |
          cd test/playwright
          bash ../../.github/scripts/retry.sh 3 npm install --no-audit --no-fund
          bash ../../.github/scripts/retry.sh 3 npx playwright install --with-deps chromium

      - name: Playwright web UI suite (no device)
        id: e2e
        env:
          STF_URL: http://127.0.0.1:7100
        run: |
          cd test/playwright
          npx playwright test ui.spec.js

      - name: Start SeaweedFS for the s3 storage backend
        if: always()
        run: |
          docker run -d --name seaweedfs -p 8333:8333 \
            chrislusf/seaweedfs:4.47 server -s3
          bash .github/scripts/wait-for-s3.sh http://127.0.0.1:8333 90

      - name: Round-trip an apk through both storage backends
        id: storage
        if: always()
        env:
          STF_LOG_DIR: stf-logs
        run: bash .github/scripts/storage-leg.sh

      - name: Derive checks from the Playwright report
        if: always()
        id: checks
        run: |
          node .github/scripts/playwright-checks.js \
            test-results/playwright/report.json playwright-checks.json

      - name: Collect logs
        if: always()
        run: |
          mkdir -p stf-logs
          node .github/scripts/stf-devices.js list > stf-logs/stf-devices.json \
            2> stf-logs/stf-devices.err || true
          if [ -f stf-logs/stf.pid ]; then
            kill "$(cat stf-logs/stf.pid)" 2>/dev/null || true
          fi

      - name: Upload logs and Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: integration-logs
          path: |
            stf-logs
            test-results/playwright
            playwright-checks.json
          retention-days: 5
          if-no-files-found: warn

      - name: Record verdict
        if: always()
        env:
          STF_UP: ${{ steps.start.outcome }}
          FAKE: ${{ steps.fakedevice.outcome }}
          E2E: ${{ steps.e2e.outcome }}
          STORAGE: ${{ steps.storage.outcome }}
          VERDICT_EXTRA: ${{ steps.checks.outputs.extra }}
        run: |
          DETAILS="stf local $STF_UP, fake devices $FAKE, web UI suite $E2E, storage $STORAGE"
          bash .github/scripts/verdict.sh integration \
            'Integration + device-less E2E (stf local + RethinkDB + Playwright + SeaweedFS)' \
            '${{ job.status }}' "$DETAILS"

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-integration
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  android:
    name: Android ${{ matrix.android }} (API ${{ matrix.api }})
    needs: [plan, build]
    runs-on: ubuntu-24.04
    # Room for two 50 minute emulator attempts plus the setup around them. A
    # healthy leg spends under three minutes in the emulator step and never
    # retries, so this ceiling is only ever paid by a leg that already failed.
    timeout-minutes: 115
    continue-on-error: true
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix: ${{ fromJSON(needs.plan.outputs.matrix) }}
    services:
      rethinkdb:
        image: rethinkdb:2.4.2
        ports:
          - 28015:28015
    steps:
      - uses: actions/checkout@v7

      - name: Free disk space
        run: |
          # A system image plus node_modules plus a 6G userdata partition does
          # not fit next to the preinstalled toolchains.
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/share/swift /opt/ghc \
            /usr/local/share/boost /usr/local/lib/android/sdk/ndk \
            /usr/local/share/powershell 2>/dev/null || true
          docker image prune -af 2>/dev/null || true
          df -h /

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          # trigger queues the event and returns, so the rule may not have been
          # applied yet. Wait for the queue to drain, then set the mode directly
          # if the node is still not writable.
          sudo udevadm settle --timeout=30 || true
          test -w /dev/kvm || {
            echo "::warning::udev kvm rule did not apply, falling back to chmod"
            sudo chmod 666 /dev/kvm
          }
          ls -l /dev/kvm
          test -w /dev/kvm

      - name: Configure a fast apt mirror
        uses: vegardit/fast-apt-mirror.sh@806c59276adfff2df420124ad5e4587f018c358a
        continue-on-error: true

      - name: Install runtime dependencies
        timeout-minutes: 12
        run: bash .github/scripts/apt-install.sh graphicsmagick libzmq5

      - name: Restore dependency cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
          key: ${{ needs.build.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: Download front-end bundle
        uses: actions/download-artifact@v8
        with:
          name: stf-bundle
          path: res/build

      # `playwright install --with-deps` shells out to apt for the browser
      # libraries, so it inherits every apt flake the runtime steps have and
      # reports it as a bare "Installation process exited with code: 100".
      # Both halves are pure network, so retry rather than red the run.
      - name: Install Playwright
        timeout-minutes: 15
        run: |
          cd test/playwright
          bash ../../.github/scripts/retry.sh 3 npm install --no-audit --no-fund
          bash ../../.github/scripts/retry.sh 3 npx playwright install --with-deps chromium

      - name: Wait for RethinkDB
        run: bash .github/scripts/wait-for-port.sh 28015 60 rethinkdb

      # The image ships cmdline-tools 12.0, which cannot parse a dotted API level
      # and writes target=android-0 into the AVD. That mis-configures gfxstream
      # and SurfaceFlinger aborts in mapper.ranchu, so every Android 17 leg
      # fails. A no-op for integer levels. See doc/ci.md.
      # Only the dotted levels need it, and the archive is 173 MiB, so the 16
      # integer legs skip the download entirely. API 36 resolves to
      # target=android-36 under both the shipped and the updated revision.
      - name: Cache the Android cmdline-tools
        if: contains(matrix.api, '.')
        uses: actions/cache@v6
        with:
          path: ~/.cache/android-cmdline-tools
          key: cmdline-tools-16111833-${{ runner.os }}-${{ runner.arch }}

      - name: Update the Android cmdline-tools
        if: contains(matrix.api, '.')
        run: bash .github/scripts/update-cmdline-tools.sh

      # Fresh Android releases are not always published as an x86_64 image yet,
      # so probe instead of hardcoding a matrix that rots.
      - name: Probe for the system image
        id: probe
        run: |
          IMAGE="system-images;android-${{ matrix.api }};${{ matrix.target }};${{ matrix.arch }}"
          echo "looking for $IMAGE"
          SDKMANAGER="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}/cmdline-tools/latest/bin/sdkmanager"
          [ -x "$SDKMANAGER" ] || SDKMANAGER=""
          if [ -z "$SDKMANAGER" ]; then
            echo "::warning::no sdkmanager found, assuming the image exists"
            echo "available=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Revision 23 prints package paths slash-separated, so normalise before
          # matching or every grep below misses and the leg fails open.
          "$SDKMANAGER" --list --channel=${{ matrix.channel == 'dev' && '2' || '0' }} 2>sdk-packages.err \
            | tr '/' ';' > sdk-packages.txt || true
          # Fail open. An sdkmanager that errors out produces an empty listing,
          # and treating that as "image not published" would let every leg
          # self-skip and the whole matrix report green with zero coverage.
          if ! grep -q "system-images;" sdk-packages.txt; then
            echo "::warning::sdkmanager listed no system images at all, assuming the image exists"
            head -20 sdk-packages.err || true
            echo "available=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if grep -qF "$IMAGE" sdk-packages.txt; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::$IMAGE is not published"
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Record a skip when the image is unpublished
        if: steps.probe.outputs.available != 'true'
        run: echo '{"image":"skip"}' > checks.json

      - name: Boot the emulator and run STF against it
        id: emulator
        if: steps.probe.outputs.available == 'true'
        continue-on-error: true
        # Cap the step, not just the job: a leg whose image will not boot can
        # otherwise sit here until the job timeout and never reach the verdict
        # and log-upload steps below.
        timeout-minutes: 50
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        env:
          STF_LOG_DIR: stf-logs
          STF_PUBLIC_IP: 127.0.0.1
          CHECKS_FILE: checks.json
          BOOT_TIMEOUT: ${{ matrix.boot }}
        with:
          api-level: ${{ matrix.api }}
          target: ${{ matrix.target }}
          channel: ${{ matrix.channel || 'stable' }}
          arch: ${{ matrix.arch }}
          # Without a device profile avdmanager builds a 320x640 160dpi screen,
          # which is smaller than the browser's device pane, so minicap has to
          # scale up and the picture ends up letterboxed in the STF UI. pixel_2
          # is 1080x1920 420dpi and ships with the SDK for every api level here.
          profile: pixel_2
          cores: 2
          ram-size: 4096M
          disk-size: 6G
          emulator-boot-timeout: ${{ matrix.boot }}
          disable-animations: true
          emulator-options: >-
            -no-window -gpu swiftshader_indirect -no-snapshot -noaudio
            -no-boot-anim -camera-back none -camera-front none
          script: bash .github/scripts/with-emulator-cleanup.sh .github/scripts/android-leg.sh

      - name: Derive the verdict
        if: always()
        id: leg
        env:
          CHECKS_FILE: checks.json
          ANDROID_VERSION: ${{ matrix.android }}
          ANDROID_API: ${{ matrix.api }}
          ANDROID_TARGET: ${{ matrix.target }}
          ANDROID_ARCH: ${{ matrix.arch }}
          EMULATOR_OUTCOME: ${{ steps.emulator.outcome }}
        run: node .github/scripts/android-verdict.js

      # The matrix is 20 legs wide and every one of them gates the run, so a 1%
      # per-leg flake rate reds one run in five however healthy STF is. Each
      # failure mode seen so far -- a boot that ran out the clock, minicap
      # frames that never reached the browser, a device STF never registered --
      # passed on the next run of the same commit, so give the leg a second go
      # and let that verdict stand.
      #
      # Gating on the verdict rather than on the emulator step's own outcome is
      # deliberate: android-leg.sh never exits non-zero for a test failure, so
      # the layer flakes all leave the step green. The plain `if` (no always())
      # is deliberate too -- it keeps the retry from firing when a setup step
      # earlier in the job failed, where there is nothing to retry and the
      # apt-install and Playwright retries are the fix instead.
      - name: Keep the first attempt before retrying
        id: keep
        if: steps.leg.outputs.status == 'fail'
        # Through the environment, not interpolated into the shell string: the
        # details come from a failing leg and end up inside double quotes.
        env:
          LEG_DETAILS: ${{ steps.leg.outputs.details }}
        run: |
          rm -rf stf-logs-attempt-1 checks-attempt-1.json
          mv stf-logs stf-logs-attempt-1 2>/dev/null || true
          mv checks.json checks-attempt-1.json 2>/dev/null || true
          # A leg killed by the step timeout can leave the emulator alive, and a
          # second one cannot bind the ports it is still holding.
          adb kill-server 2>/dev/null || true
          pkill -f qemu-system 2>/dev/null || true
          # The retry costs no extra disk: the action creates the AVD as
          # `avdmanager create --force -n test`, so the second attempt replaces
          # the first one's 6G userdata rather than adding to it, and the system
          # image is already downloaded. Print it anyway, because a retry that
          # dies of a full disk would read as a leg that failed twice.
          df -h /
          echo "::warning::Android ${{ matrix.android }} (API ${{ matrix.api }}) failed: ${LEG_DETAILS}. Retrying once."

      - name: Retry the leg once
        id: emulator_retry
        if: steps.keep.outcome == 'success'
        continue-on-error: true
        timeout-minutes: 50
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d # v2.38.0
        env:
          STF_LOG_DIR: stf-logs
          STF_PUBLIC_IP: 127.0.0.1
          CHECKS_FILE: checks.json
          BOOT_TIMEOUT: ${{ matrix.boot }}
        with:
          api-level: ${{ matrix.api }}
          target: ${{ matrix.target }}
          channel: ${{ matrix.channel || 'stable' }}
          arch: ${{ matrix.arch }}
          profile: pixel_2
          cores: 2
          ram-size: 4096M
          disk-size: 6G
          emulator-boot-timeout: ${{ matrix.boot }}
          disable-animations: true
          emulator-options: >-
            -no-window -gpu swiftshader_indirect -no-snapshot -noaudio
            -no-boot-anim -camera-back none -camera-front none
          script: bash .github/scripts/with-emulator-cleanup.sh .github/scripts/android-leg.sh

      - name: Derive the verdict again
        if: always() && steps.emulator_retry.outcome != 'skipped'
        id: leg2
        env:
          CHECKS_FILE: checks.json
          ANDROID_VERSION: ${{ matrix.android }}
          ANDROID_API: ${{ matrix.api }}
          ANDROID_TARGET: ${{ matrix.target }}
          ANDROID_ARCH: ${{ matrix.arch }}
          EMULATOR_OUTCOME: ${{ steps.emulator_retry.outcome }}
          LEG_ATTEMPT: '2'
        run: node .github/scripts/android-verdict.js

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: android-logs-api-${{ matrix.api }}-${{ matrix.target }}
          path: |
            stf-logs
            stf-logs-attempt-1
            checks.json
            checks-attempt-1.json
            test-results/playwright
          retention-days: 5
          if-no-files-found: warn

      # The retried verdict wins when there is one. GitHub's `||` falls through
      # on an empty string, so a leg that never retried reads its first verdict.
      - name: Record verdict
        if: always()
        env:
          VERDICT_GROUP: android
          VERDICT_EXTRA: ${{ steps.leg2.outputs.extra || steps.leg.outputs.extra }}
        run: |
          bash .github/scripts/verdict.sh 'android-${{ matrix.api }}' \
            'Android ${{ matrix.android }} (API ${{ matrix.api }})' \
            '${{ steps.leg2.outputs.status || steps.leg.outputs.status }}' \
            '${{ steps.leg2.outputs.details || steps.leg.outputs.details }}'

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-android-${{ matrix.api }}-${{ matrix.target }}
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  compose:
    name: Compose
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      COMPOSE_FILE: docker-compose.yaml:test/compose/docker-compose.yaml
      COMPOSE_PROJECT_NAME: stf-compose-ci
      STF_IMAGE: stf-compose:ci
      STF_RESTART_POLICY: "no"
      STF_SECRET: stf-compose-test-secret
      STF_ADMIN_EMAIL: ci_user@ci.local
      STF_ADMIN_NAME: ci_user
    steps:
      - uses: actions/checkout@v7

      - name: Make room for the Docker image and AVD
        run: |
          sudo rm -rf /usr/share/dotnet /usr/share/swift /opt/ghc \
            /usr/local/share/boost /usr/local/lib/android/sdk/ndk
          df -h /

      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - uses: docker/setup-buildx-action@v4

      - name: Build the STF image from this checkout
        id: image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          load: true
          tags: stf-compose:ci
          cache-from: type=gha,scope=compose-amd64
          cache-to: type=gha,mode=max,scope=compose-amd64

      - name: Install the ADB relay and Playwright
        timeout-minutes: 15
        run: |
          bash .github/scripts/apt-install.sh socat
          cd test/playwright
          bash ../../.github/scripts/retry.sh 3 npm install --no-audit --no-fund
          bash ../../.github/scripts/retry.sh 3 npx playwright install --with-deps chromium

      - name: Validate and pull the Compose services
        run: |
          export STF_ADB_KEYS="$HOME/.android"
          mkdir -p test-results/compose
          docker compose config > test-results/compose/config.yaml
          docker compose pull rethinkdb adb proxy

      - name: Enable KVM
        run: |
          sudo chmod 666 /dev/kvm
          test -w /dev/kvm

      - name: Boot an AVD and test the Compose stack
        id: emulator
        timeout-minutes: 25
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d
        with:
          api-level: 36
          target: default
          arch: x86_64
          profile: pixel_2
          cores: 2
          ram-size: 4096M
          disk-size: 6G
          emulator-boot-timeout: 720
          disable-animations: true
          emulator-options: >-
            -no-window -gpu swiftshader_indirect -no-snapshot -noaudio
            -no-boot-anim -camera-back none -camera-front none
          script: bash .github/scripts/with-emulator-cleanup.sh .github/scripts/compose-leg.sh

      - name: Collect Compose diagnostics
        if: always()
        run: |
          export STF_ADB_KEYS="$HOME/.android"
          mkdir -p test-results/compose
          docker compose ps -a > test-results/compose/containers.txt
          docker compose logs --no-color > test-results/compose/services.log 2>&1

      - name: Tear down the Compose stack
        id: teardown
        if: always()
        run: |
          export STF_ADB_KEYS="$HOME/.android"
          docker compose down --volumes --remove-orphans

      - name: Derive checks from the Playwright report
        if: always()
        id: checks
        run: |
          node .github/scripts/playwright-checks.js \
            test-results/playwright/report.json test-results/compose/playwright-checks.json

      - name: Upload logs and Playwright evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: compose-playwright-results
          path: |
            test-results/compose
            test-results/playwright
          retention-days: 5
          if-no-files-found: warn

      - name: Record verdict
        if: always()
        env:
          IMAGE: ${{ steps.image.outcome }}
          E2E: ${{ steps.emulator.outcome }}
          TEARDOWN: ${{ steps.teardown.outcome }}
          TEST_COUNT: ${{ fromJSON(steps.checks.outputs.extra || '{}').playwright_total || 0 }}
          VERDICT_EXTRA: ${{ steps.checks.outputs.extra }}
        run: |
          DETAILS="${TEST_COUNT} Playwright tests, image $IMAGE, Compose/AVD $E2E, teardown $TEARDOWN"
          bash .github/scripts/verdict.sh compose \
            'Docker Compose + AVD + Playwright (Android 16, API 36)' \
            '${{ job.status }}' "$DETAILS"

      - name: Upload verdict
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: verdict-compose
          path: verdict.json
          retention-days: 3
          if-no-files-found: warn

  report:
    name: Report
    needs: [plan, build, lint, unit, component, integration, android, compose]
    if: always()
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'

      - name: Download verdicts
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: verdicts
          pattern: verdict-*
          merge-multiple: false

      - name: Render report
        id: render
        env:
          # Any tier or leg expected here that produced no verdict is reported
          # as a failure rather than quietly vanishing from the table. The
          # Android side comes from the same file the matrix is built from, so
          # adding a leg cannot leave it unenforced.
          EXPECTED_TIERS: build lint unit component integration compose
          EXPECTED_ANDROID_FILE: .github/android-matrix.json
        run: node .github/scripts/render-report.js verdicts report.md

      - name: Job summary
        if: always()
        run: cat report.md >> "$GITHUB_STEP_SUMMARY"

      - name: Stage the report for the commenter
        if: github.event_name == 'pull_request'
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          mkdir -p ci-report
          cp report.md ci-report/report.md
          echo "$PR_NUMBER" > ci-report/pr-number

      - name: Upload the staged report
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: ci-report
          path: ci-report
          retention-days: 3
          if-no-files-found: error

      - name: Fail if any tier failed
        run: |
          if [ "${{ steps.render.outputs.failed }}" != "0" ]; then
            echo "::error::${{ steps.render.outputs.failed }} CI tier(s) failed"
            exit 1
          fi
