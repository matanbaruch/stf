{
  "comment": [
    "Single source of truth for the Android matrix: the android job builds its",
    "legs from this, and the report job uses it to decide which legs must have",
    "reported. Keep api values unique.",
    "",
    "target: `default` is the smallest image and the only AOSP one published for",
    "every level here. Every API from 21 to 36 has a `default` x86 or x86_64",
    "image, so the range below has no gaps. Older images boot slower, hence the",
    "larger boot budgets.",
    "",
    "arch: x86_64 from API 26 up, x86 below it. API 21 is the first level with",
    "an x86_64 image at all, and API 24's x86_64 image never finishes booting on",
    "the current emulator binaries (it sat for 50 minutes inside",
    "android-emulator-runner without reaching sys.boot_completed), so the whole",
    "pre-26 range uses the 32 bit image. minicap and minitouch both ship x86",
    "prebuilts, so STF works on it.",
    "",
    "API 21 is the floor, and it is STF's own prebuilts rather than CI's. Android",
    "4.1 to 4.4 boot and STF registers them, then the device never becomes",
    "usable: minitouch 1.3.2 cannot link on 4.x, and 4.1 additionally crashes",
    "STFService. Both were measured, see doc/ci.md.",
    "",
    "The minor-versioned levels 36.1 and 37.x need the cmdline-tools update in",
    "ci.yml: the runner image ships 12.0, which cannot parse a dotted api like",
    "37.1 and writes target=android-0 into the AVD. See doc/ci.md.",
    "",
    "target for those rows: the AOSP repo stops at android-36;default, so 36.1",
    "and 37.x have no `default` image. 36.1 and 37.0 have a plain google_apis",
    "one; 37.1 and 37.2 ship only the 16 KB page size variant. No playstore",
    "variant is usable here because they block adb root, which minitouch needs.",
    "",
    "37.2 is published only on the dev SDK channel, hence its channel key.",
    "--channel is cumulative, so the stable legs are unaffected. The 37.2 betas",
    "are deliberately excluded: GA supersedes them, and Google prunes beta",
    "images after GA, which would make the leg self-skip green with no coverage."
  ],
  "include": [
    {"android": "5.0", "api": "21", "target": "default", "arch": "x86", "boot": 900},
    {"android": "5.1", "api": "22", "target": "default", "arch": "x86", "boot": 900},
    {"android": "6.0", "api": "23", "target": "default", "arch": "x86", "boot": 900},
    {"android": "7", "api": "24", "target": "default", "arch": "x86", "boot": 900},
    {"android": "7.1", "api": "25", "target": "default", "arch": "x86", "boot": 900},
    {"android": "8", "api": "26", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "8.1", "api": "27", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "9", "api": "28", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "10", "api": "29", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "11", "api": "30", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "12", "api": "31", "target": "default", "arch": "x86_64", "boot": 900},
    {"android": "12L", "api": "32", "target": "default", "arch": "x86_64", "boot": 720},
    {"android": "13", "api": "33", "target": "default", "arch": "x86_64", "boot": 720},
    {"android": "14", "api": "34", "target": "default", "arch": "x86_64", "boot": 720},
    {"android": "15", "api": "35", "target": "default", "arch": "x86_64", "boot": 720},
    {"android": "16", "api": "36", "target": "default", "arch": "x86_64", "boot": 720},
    {"android": "16.1", "api": "36.1", "target": "google_apis", "arch": "x86_64", "boot": 720},
    {"android": "17", "api": "37.0", "target": "google_apis", "arch": "x86_64", "boot": 900},
    {"android": "17", "api": "37.1", "target": "google_apis_ps16k", "arch": "x86_64", "boot": 900},
    {"android": "17", "api": "37.2", "target": "google_apis_ps16k", "arch": "x86_64", "boot": 900, "channel": "dev"}
  ]
}
